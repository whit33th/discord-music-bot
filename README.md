# discord-music-bot

To install dependencies:

```bash
bun install
```

To run:

```bash
bun run start
```

If voice playback stutters, use the default `Node + tsx` runtime above.

```bash
# compare with the old Bun runtime if needed
bun run start:bun
```
